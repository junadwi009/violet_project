"""File ingestion (Phase 3c).

Extracts text from uploaded documents (CSV, XLSX, PDF, DOCX, TXT/MD/JSON) and runs OCR on images
via an OpenRouter vision model, so uploaded files become a data source for chat and skills.
"""
