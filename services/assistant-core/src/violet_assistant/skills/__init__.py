"""Skills framework (Phase 3).

Config-driven capabilities (``configs/skills/*.json``) that let Violet produce renderable
artifacts — charts (Chart.js specs) and self-contained interactive HTML dashboards. Rule-based
detection picks a skill from the user's message; a coding model generates the artifact.
"""
