"""Specialized sub-agents (Phase 3e).

Config-driven agents (``configs/agents/*.json``), each with its own model + system prompt, that
Violet can hand a task off to (explicit selection or rule-based auto-detection).
"""
